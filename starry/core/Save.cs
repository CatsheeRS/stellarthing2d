using System;
using System.IO;
namespace starry;

/// <summary>
/// manages all things saving and serialization
/// </summary>
public static class Save {
    /*/// <summary>
    /// the current save
    /// </summary>
    public static string universe { get; set; } = "";
    /// <summary>
    /// the folder where saves are going to
    /// </summary>
    public static string savedir { get => Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData, Environment.SpecialFolderOption.Create), universe); }

    /// <summary>
    /// saves the properties of the class. the class has to be public and static.
    /// </summary>
    /// <typeparam name="T"></typeparam>
    public static void save<T>()
    {

    }

    /// <summary>
    /// serializes the properties of the class. the class has to be public and static.
    /// </summary>
    public static string serialize<T>()
    {
        // first get the properties
        Type type = typeof(T);

    }*/
}